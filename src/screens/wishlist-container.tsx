import SavedListContainer from "./saved-list-container";

const WishlistContainer = () => {
  return <SavedListContainer type="watchlist" showHeader title="Wishlist" />;
};

export default WishlistContainer;
